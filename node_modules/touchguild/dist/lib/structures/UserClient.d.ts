/** @module UserClient */
import { Client } from "./Client";
import { User } from "./User";
import { APIBotUser } from "../Constants";
import { JSONUserClient } from "../types/json";
/** UserClient represents the logged bot's user. */
export declare class UserClient extends User {
    /** Client User Bot ID */
    botID: string;
    /** ID of the bot's owner. */
    ownerID: string;
    /** Boolean that shows if the user is a bot or not.
     * @defaultValue true
     */
    bot: true;
    /**
     * @param data raw data.
     * @param client client.
     */
    constructor(data: APIBotUser, client: Client);
    toJSON(): JSONUserClient;
    protected update(data: APIBotUser): void;
}
