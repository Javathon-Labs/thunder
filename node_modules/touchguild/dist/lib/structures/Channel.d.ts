/** @module Channel */
import { Client } from "./Client";
import { Base } from "./Base";
import { JSONChannel } from "../types/json";
import type { APIGuildChannel } from "../Constants";
import { AnyChannel, EditChannelOptions } from "../types/channel";
/** Represents a channel. */
export declare class Channel extends Base<string> {
    /** Channel type */
    type: string;
    /** Channel name */
    name: string | null;
    /**
     * @param data raw data
     * @param client client
     */
    constructor(data: APIGuildChannel, client: Client);
    static from<T extends AnyChannel = AnyChannel>(data: APIGuildChannel, client: Client): T;
    toJSON(): JSONChannel;
    /** Edit the channel. */
    edit(options: EditChannelOptions): Promise<Channel>;
    /** Delete the channel. */
    delete(): Promise<void>;
}
