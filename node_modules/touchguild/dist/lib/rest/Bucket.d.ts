/** @module Bucket */
/**
 * Credits to Eris & Oceanic.js
 * Edited by PG.
 * https://github.com/abalabahaha/eris/blob/dev/lib/util/Bucket.js
 */
/// <reference types="node" />
/** A bucket. */
export default class Bucket {
    #private;
    interval: number;
    lastReset: number;
    lastSend: number;
    latencyRef: {
        latency: number;
    };
    reservedTokens: number;
    timeout: NodeJS.Timeout | null;
    tokenLimit: number;
    tokens: number;
    constructor(tokenLimit: number, interval: number, options?: {
        latencyRef?: {
            latency: number;
        };
        reservedTokens?: number;
    });
    private check;
    /**
     * Add an item to the queue.
     * @param func The function to queue.
     * @param priority If true, the item will be added to the front of the queue.
     */
    queue(func: () => void, priority?: boolean): void;
}
