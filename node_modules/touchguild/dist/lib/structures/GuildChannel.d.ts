/** @module GuildChannel */
import { Client } from "./Client";
import { Base } from "./Base";
import { Channel } from "./Channel";
import { EditChannelOptions } from "../types/channel";
import type { APIGuildChannel } from "../Constants";
import { JSONGuildChannel } from "../types/json";
/** Represents a guild channel. */
export declare class GuildChannel extends Base<string> {
    /** Channel type */
    type: string;
    /** Channel name */
    name: string;
    /** Channel description */
    description: string | null;
    /** When this channel was created. */
    createdAt: Date;
    /** ID of the member who created this channel. */
    creatorID: string;
    /** Timestamp at which this channel was last edited. */
    editedTimestamp: Date | null;
    /** Server ID */
    guildID: string;
    /** ID of the parent category. */
    parentID: string | null;
    /** ID of the category the channel is in. */
    categoryID: number | null;
    /** ID of the group the channel is in. */
    groupID: string;
    isPublic: boolean;
    /** ID of the member that archived the channel (if archived) */
    archivedBy: string | null;
    /** When the channel was last archived. */
    archivedAt: Date | null;
    /** Channel visibility */
    visibility: string;
    /**
     * @param data raw data
     * @param client client
     */
    constructor(data: APIGuildChannel, client: Client);
    toJSON(): JSONGuildChannel;
    protected update(data: APIGuildChannel): void;
    /** Edit the channel. */
    edit(options: EditChannelOptions): Promise<Channel>;
    /** Delete the channel. */
    delete(): Promise<void>;
    /** Archive the channel */
    archive(): Promise<void>;
    /** Unarchive the channel */
    restore(): Promise<void>;
}
