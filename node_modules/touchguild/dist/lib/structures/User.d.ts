/** @module User */
import { Client } from "./Client";
import { Base } from "./Base";
import { UserTypes, APIUser, APIGuildMember, APIUserSummary } from "../Constants";
import { JSONUser } from "../types/json";
/** Represents a user. */
export declare class User extends Base<string> {
    /** User type */
    type: UserTypes | null;
    /** User's username. */
    username: string;
    /** Current avatar url of the user. */
    avatarURL: string | null;
    /** Current banned url of the user. */
    bannerURL: string | null;
    /** When the user account was created. */
    createdAt: Date;
    /** If true, the user is a bot. */
    bot: boolean;
    /**
     * @param data raw data.
     * @param client client.
     */
    constructor(data: APIUser, client: Client);
    toJSON(): JSONUser;
    protected update(d: APIUser | APIGuildMember | APIUserSummary): void;
}
